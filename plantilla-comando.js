const { EmbedBuilder } = require('discord.js')

module.exports = {
  name: 'nombre del comando',
  alias ['alias del comando', 'puede haber mas de un alias xd'],
  
 run (client, message, args) {
  //código del comando
 }
}
